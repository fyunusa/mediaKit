import FontStudio from "@/components/tools/FontStudio";

export const metadata = {
  title: "Font Studio — MediaKit",
  description:
    "Test any Google Font live with full typography controls, discover fonts by mood and category, and get curated pairings — all in one tool.",
};

export default function Page() {
  return <FontStudio />;
}
