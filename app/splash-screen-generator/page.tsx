import SplashScreenGenerator from "@/components/tools/SplashScreenGenerator";

export const metadata = {
  title: "Splash Screen Generator — MediaKit",
  description: "Generate all iOS and Android splash screen densities with background color picker.",
};

export default function Page() {
  return <SplashScreenGenerator />;
}
