import AppIconGenerator from "@/components/tools/AppIconGenerator";

export const metadata = {
  title: "App Icon Generator — MediaKit",
  description: "Generate all iOS and Android app icon sizes from one image.",
};

export default function Page() {
  return <AppIconGenerator />;
}
